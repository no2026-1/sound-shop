<!DOCTYPE html>
<html lang="th">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Sound Shop - JBL</title>
<link href="/css/output.css" rel="stylesheet">
</head>
<body class="bg-gray-100 font-sans">

<!-- Navbar -->
<nav class="bg-gray-900 text-white px-6 py-4 flex justify-between items-center shadow-lg sticky top-0 z-50">
  <h1 class="text-3xl font-bold">🎧 Sound Shop</h1>
  <div class="space-x-6 flex items-center">
    <a href="/" class="hover:text-gray-300 font-semibold">หน้าแรก</a>
    <a href="/cart" class="hover:text-gray-300 font-semibold relative">
      ตะกร้า
      <% if(cartCount>0){ %>
        <span class="absolute -top-2 -right-3 bg-red-600 text-white text-xs rounded-full px-2"><%= cartCount %></span>
      <% } %>
    </a>
  </div>
</nav>

<!-- Banner / Slider -->
<!--<section class="relative w-full h-80 md:h-96 overflow-hidden mx-4 md:mx-0 mt-6 rounded-2xl shadow-xl">
  <div class="absolute inset-0 flex transition-transform duration-700 ease-in-out" id="slider">
  -->
    <!-- Slide 1 -->
   <!---- <div class="w-full flex-shrink-0 relative">
      <img src="/images/jbl_charge_5.jpg" alt="JBL Charge 5" class="w-full h-80 md:h-96 object-cover brightness-75 rounded-2xl">
      <div class="absolute inset-0 flex flex-col justify-center items-start px-6 md:px-16 text-white">
        <h2 class="text-3xl md:text-5xl font-bold mb-2 drop-shadow-lg">โปรโมชั่น JBL Charge 5</h2>
        <p class="text-lg md:text-2xl mb-4 drop-shadow-lg">ลดพิเศษ 10% วันนี้เท่านั้น!</p>
        <a href="#products" class="bg-blue-600 hover:bg-blue-700 px-5 py-2 rounded-xl font-semibold shadow-lg transition">ดูสินค้า</a>
      </div>
    </div>
  -->
    <!-- Slide 2 -->
    <!--<div class="w-full flex-shrink-0 relative">
      <img src="/images/jbl_flip_6.jpg" alt="JBL Flip 6" class="w-full h-80 md:h-96 object-cover brightness-75 rounded-2xl">
      <div class="absolute inset-0 flex flex-col justify-center items-start px-6 md:px-16 text-white">
        <h2 class="text-3xl md:text-5xl font-bold mb-2 drop-shadow-lg">JBL Flip 6</h2>
        <p class="text-lg md:text-2xl mb-4 drop-shadow-lg">ซื้อ 1 แถม 1!</p>
        <a href="#products" class="bg-purple-600 hover:bg-purple-700 px-5 py-2 rounded-xl font-semibold shadow-lg transition">ดูสินค้า</a>
      </div>
    </div>
  -->
    <!-- Slide 3 -->
  <!---- <div class="w-full flex-shrink-0 relative">
      <img src="/images/jbl_boombox_2.jpg" alt="JBL Boombox 2" class="w-full h-80 md:h-96 object-cover brightness-75 rounded-2xl">
      <div class="absolute inset-0 flex flex-col justify-center items-start px-6 md:px-16 text-white">
        <h2 class="text-3xl md:text-5xl font-bold mb-2 drop-shadow-lg">JBL Boombox 2</h2>
        <p class="text-lg md:text-2xl mb-4 drop-shadow-lg">จัดส่งฟรีทั่วประเทศ</p>
        <a href="#products" class="bg-yellow-600 hover:bg-yellow-700 px-5 py-2 rounded-xl font-semibold shadow-lg transition">ดูสินค้า</a>
      </div>
    </div>

  </div>
</section>
-->
<script>
  let index = 0;
  const slides = document.getElementById('slider');
  const total = slides.children.length;
  setInterval(() => {
    index = (index + 1) % total;
    slides.style.transform = `translateX(-${index*100}%)`;
  }, 4000);
</script>

<!-- Search + Filter + Sort -->
<section class="container mx-auto px-6 py-8">
  <div class="flex flex-col md:flex-row gap-4 mb-8 justify-center items-center flex-wrap">

    <form method="GET" action="/" class="flex flex-1">
      <input type="text" name="search" placeholder="ค้นหา JBL รุ่น..." class="flex-1 border rounded-l-xl px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-400">
      <button type="submit" class="bg-blue-600 text-white px-4 py-2 rounded-r-xl hover:bg-blue-700">ค้นหา</button>
    </form>

    <form method="GET" action="/" class="flex items-center gap-2 flex-wrap">
      <select name="price" class="border rounded-xl px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-400">
        <option value="">ราคาทั้งหมด</option>
        <option value="0-1500000">0 - 1,500,000 LAK</option>
        <option value="1500001-3000000">1,500,001 - 3,000,000 LAK</option>
        <option value="3000001-5000000">3,000,001 - 5,000,000 LAK</option>
      </select>
      <select name="category" class="border rounded-xl px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-400">
        <option value="">หมวดหมู่ทั้งหมด</option>
        <option value="ลำโพง">ลำโพง</option>
        <option value="Soundbar">Soundbar</option>
        <option value="หูฟัง">หูฟัง</option>
      </select>
      <select name="sort" class="border rounded-xl px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-400">
        <option value="">จัดเรียง</option>
        <option value="low">ราคาต่ำ → สูง</option>
        <option value="high">ราคาสูง → ต่ำ</option>
      </select>
      <button type="submit" class="bg-gray-600 text-white px-4 py-2 rounded-xl hover:bg-gray-700">Filter</button>
    </form>

  </div>
</section>

<!-- Products -->
<section id="products" class="container mx-auto px-6 py-8">
  <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-8">
    <% products.forEach(product => { %>
      <div class="bg-white rounded-2xl shadow-lg hover:shadow-2xl transition p-4 flex flex-col">
        <img src="<%= product.image %>" class="rounded-2xl mb-4 object-cover h-48 w-full" alt="<%= product.name %>">
        <h4 class="text-xl font-bold mb-2"><%= product.name %></h4>
        <p class="text-gray-600 mb-4 font-semibold">ราคา <%= product.price.toLocaleString() %> LAK</p>
        <a href="/add-to-cart/<%= product.id %>" class="mt-auto bg-blue-600 text-white py-2 rounded-xl hover:bg-blue-700 text-center font-semibold transition">ใส่ตะกร้า</a>
      </div>
    <% }) %>
  </div>
</section>

<!-- Footer -->
<footer class="bg-gray-900 text-white text-center py-6 mt-12">
  &copy; 2026 Sound Shop. สงวนลิขสิทธิ์.
</footer>
</body>
</html>
